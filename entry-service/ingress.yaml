apiVersion: extensions/v1beta1
kind: Ingress
metadata: 
  name: ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: cleanaux.ingress.com
    http:
      paths:
      - backend:
          serviceName: entry-service
          servicePort: 8000